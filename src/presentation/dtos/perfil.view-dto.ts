/*
import { Student } from '../../domain/entities/students.entity';

export class StudentsViewModel {
    
    id: number;
    cpf:string;
    first_name: string;
    email: string;

    public toViewModel(student: Student): StudentsViewModel {
        const viewModel = new StudentsViewModel();
        viewModel.id = student.id;
        viewModel.cpf = student.cpf;
        viewModel.first_name = student.first_name
        viewModel.email = student.email;
        return viewModel;
    }

}
    */